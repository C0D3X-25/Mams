<UserControl x:Class="Mams.src.views.userControls.UCLabelTextBoxSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mams.src.views.userControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="root">
    
    <!--Label + TextBoxSearch-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Label x:Name="labelTextBox_lbl" 
                Grid.Column="0" Grid.Row="0" 
                Content="{Binding label, ElementName=root}" 
                FontSize="16" 
                VerticalAlignment="Center" HorizontalAlignment="Right"

        ></Label>

        <ComboBox x:Name="searchComboBox"
                Grid.Column="1" Grid.Row="0" 
                Margin="5,0,5,0" 
                FontSize="16" 
                VerticalAlignment="Center"
                MinHeight="25" MinWidth="200"
                ItemsSource="{Binding list_items, ElementName=root}"
                SelectedItem="{Binding selected_item, ElementName=root}"
                  
        ></ComboBox>
                <!--IsEditable="False"-->

    </Grid>
    <!--Label + TextBoxSearch-->
</UserControl>
